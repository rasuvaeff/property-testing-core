# Domain vocabulary Vale's dictionary does not know.
#
# Entries are regular expressions, and case matters twice over: an entry
# teaches Vale.Spelling the word AND teaches Vale.Terms the one correct
# casing. A bare `backoff` therefore makes Vale.Terms reject "Backoff" at the
# start of a sentence or heading — verified live 2026-08-09, which is why
# every common noun below is written with a bracketed initial. Only names
# with genuinely fixed casing (PHPUnit, flatMap, PROPERTY_DB) are spelled
# exactly.
#
# Deliberately NOT listed: shrink, shrinking, shrunk, arbitrary. The
# dictionary already accepts them, and listing them would put them under
# Vale.Terms for no gain.
[Aa]rbitraries
[Aa]rity
[Aa]utoloader
[Bb]ackoff
[Cc]odecov
[Cc]ombinators
[Dd]erandomiz(?:e|ed|es|ing)
[Dd]ocblock(?:s)?
[Ee]num
[Ii]njective
[Ii]nts
[Ii]nvokable
[Mm]isconfigured
[Mm]onorepo
[Mm]ultibyte
[Nn]amespace
[Pp]ostcondition
[Rr]egexes
[Rr]ollout
[Ss]ubtree
[Uu]nshrunk
[Uu]ntrusted
[Vv]ariadic
[Vv]alidator
[Ww]alkthrough
flatMap
# The Java property-testing library, cited on the from-a-class page for its
# type-driven @ForAll.
jqwik
# Surname of the Swarm Testing paper's first author (ISSTA 2012), cited on the
# swarm page. A proper noun the dictionary does not carry.
Groce
PCRE
PHPUnit
PROPERTY_DB
Testo
# The two PHP Redis clients the corpus can talk through, and the framework
# whose DSN shape the `redis://` parsing follows. Lowercase `predis` is the
# package's own spelling (predis/predis), so it is not a sentence-initial
# variant the way the common nouns above are.
predis
Symfony
# The URI component a `redis://user:pass@host` DSN puts the credentials in —
# RFC 3986's own name for it, and the one the refusal message uses.
userinfo
